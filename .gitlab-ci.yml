stages:
  - build
  - cleanup

build:
  stage: build
  script:
      - cp .env-dist .env
      - docker-compose run --rm php composer install
      - docker-compose up -d
      - docker-compose stop

cleanup:
  stage: cleanup
  script:
      - docker-compose kill
      - docker-compose rm -fv
  when: always